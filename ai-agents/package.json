{
  "name": "ai-agents",
  "version": "1.0.0",
  "description": "Multi-agent strategy AI with 1-hour play + 10-min reflection cycles. Uses GitHub Models (gpt-4o-mini) via 4 separate PATs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "node server.js --verbose"
  },
  "dependencies": {
    "dotenv":   "^16.4.5",
    "express":  "^4.19.2"
  },
  "engines": { "node": ">=18" }
}
